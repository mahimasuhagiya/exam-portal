spring.application.name=examportal
spring.datasource.url=jdbc:sqlserver://localhost\\SQLEXPRESS;databaseName=exam;encrypt=true;trustServerCertificate=true
server.port=8080
spring.datasource.username=mahima
spring.datasource.password=mahima25
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# JPA and Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update  
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect
spring.jpa.properties.hibernate.format_sql=true

#multipart uploads enabling
spring.servlet.multipart.enabled=true

#max file  size
spring.servlet.multipart.max-file-size=1000MB

#spring.servlet.multipart.max-request-size=50MB

#threshold after which files are written to disk
#spring.servlet.multipart.file-size-threshold=1kb